services:
  # ─── Towchen API ──────────────────────────────────────────
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: towchen_api
    restart: unless-stopped
    environment:
      # ── Database ──────────────────────────────────────────
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: TOW_TEST_1
      DB_USER: root
      DB_PASS: 2026@Password
      DATABASE_URL: postgresql://root:2026@Password@postgres:5432/TOW_TEST_1

      # ── JWT ───────────────────────────────────────────────
      JWT_SECRET: TOWCHEN_SECRET
      JWT_EXPIRES_IN: 7d

      # ── Cloudflare R2 Storage ─────────────────────────────
      STORAGE_PROVIDER: cloudflare-R2
      R2_ACCOUNT_ID: f68ec044685c09f59025331b44fd47ab
      R2_ACCESS_KEY_ID: 9a719d2be18210ebff6d7ac13f08a711
      R2_SECRET_ACCESS_KEY: dfab8a8612ea5fa17e636375ccca2a7778835924a6b56705e61eaaa9d9e37965
      R2_BUCKET_NAME: towchen
      R2_PUBLIC_URL: https://pub-88bff11829e24671b851121c59781ac4.r2.dev

      # ── Twilio SMS ────────────────────────────────────────
      TWILIO_ACCOUNT_SID: AC53cc78e6deb8b97c4428eeeb0c0a4120
      TWILIO_AUTH_TOKEN: 60df8a68acde99d02db3ea2b4479bec4
      TWILIO_PHONE_NUMBER: "+15342026289"

      # ── Mail ──────────────────────────────────────────────
      MAIL_PROVIDER: nodemailer
      MAIL_HOST: smtp.gmail.com
      MAIL_PORT: 587
      MAIL_USER: jinvixmanage@gmail.com
      MAIL_PASS: uzwqvaplajbmfkyt
      MAIL_FROM: jinvixmanage@gmail.com

    ports:
      - '3000:3000'
    # Run Prisma migrations before starting the app
    command: >
      sh -c "npx prisma migrate deploy && node dist/main"
    networks:
      - proxy_network

networks:
  proxy_network:
    external: true
